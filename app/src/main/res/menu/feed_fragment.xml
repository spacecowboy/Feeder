<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.FeedActivity">

    <!-- Overriding these values in fragment -->
    <item
        android:id="@+id/action_only_unread"
        android:checkable="true"
        android:checked="true"
        android:icon="@drawable/ic_visibility_white_24dp"
        android:orderInCategory="1"
        android:title="@string/show_all_items"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/action_edit_feed"
        android:icon="@drawable/ic_edit_white_24dp"
        android:title="@string/edit_feed"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_add_templated"
        android:icon="@drawable/ic_edit_white_24dp"
        android:title="@string/new_based_on_this"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_delete_feed"
        android:icon="@drawable/ic_delete_white_24dp"
        android:title="@string/delete_feed"
        app:showAsAction="never" />
</menu>
