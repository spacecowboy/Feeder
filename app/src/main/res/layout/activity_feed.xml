<!-- A DrawerLayout is intended to be used as the top-level content view using match_parent for both width and height to consume the full space available. -->
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FeedActivity">

    <com.nononsenseapps.feeder.views.DrawShadowFrameLayout
        android:id="@+id/main_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false"
        app:shadowDrawable="@drawable/header_shadow">
        <!-- As the main content view, the view below consumes the entire
             space available using match_parent in both dimensions. -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <FrameLayout
                android:id="@+id/container"
                android:layout_width="match_parent"
                android:layout_height="match_parent"/>

            <include layout="@layout/sync_progress_indicator"
                     android:id="@+id/sync_indicator_1"
                     android:layout_width="match_parent"
                     android:layout_height="wrap_content"
                     android:visibility="gone"
                     android:minHeight="3dp"/>

            <include layout="@layout/include_floating_add_button"/>

        </FrameLayout>

        <LinearLayout
            style="@style/HeaderBar"
            android:id="@+id/headerbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clickable="true"
            android:orientation="vertical">

            <include layout="@layout/toolbar_actionbar"/>
            <!-- placeholder, empty except on L -->

            <TextView
                android:id="@+id/filters_box"
                android:visibility="gone"
                android:layout_marginTop="?actionBarOverlayTopOffset"
                android:paddingLeft="@dimen/keyline_content_start_minus_16dp"
                android:paddingRight="@dimen/keyline_content_start_minus_16dp"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:background="@color/primary"
                style="@style/TextAppearance.Body"
                android:textColor="@android:color/primary_text_dark"
                android:text="Add some filter actions here"/>
        </LinearLayout>

        <include layout="@layout/sync_progress_indicator"
                 android:id="@+id/sync_indicator_2"
                 android:layout_width="match_parent"
                 android:layout_height="wrap_content"
                 android:minHeight="3dp"
                 android:visibility="gone"
                 android:layout_marginTop="?android:attr/actionBarSize"/>

    </com.nononsenseapps.feeder.views.DrawShadowFrameLayout>

    <!-- Nav drawer -->
    <include layout="@layout/navdrawer_for_ab_overlay"/>

</android.support.v4.widget.DrawerLayout>
